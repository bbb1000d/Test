# Prompt Templates

This directory contains human-readable prompt templates that can be adapted for
production usage. They mirror the helper methods in `StudyOrchestrator` and can
be used to fine-tune prompts for specific institutions or courses.
